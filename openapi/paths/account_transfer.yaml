get:
  tags:
    - account
  summary: Internal fund transfer
  description: Perform internal fund transfer
  operationId: fundTransfer
  responses:
    '200':
      description: successful operation
      content:
        application/json:
          schema:
            $ref: ..\components\schemas\transfer.yaml
